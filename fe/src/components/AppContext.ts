import { createContext } from 'react';
import { Company } from '../types/Company';
import { Job } from '../types/Job';


export const AppContext = createContext("vars");

// export const CompaniesContext = createContext<Company[]>([]);
